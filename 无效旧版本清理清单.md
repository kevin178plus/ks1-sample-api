# 无效/旧版本文件清理清单

**整理时间：** 2026-05-10 19:45:00

---

## 一、建议删除的 Python 文件

### 1.1 过时的主服务文件

| 文件 | 原因 | 影响 |
|------|------|------|
| `local_api_proxy.py` | **v1 原始单文件版**，功能已被 multi_free_api_proxy 目录下的版本完全替代 | 无影响，推荐删除 |
| `multi_free_api_proxy/multi_free_api_proxy.py` | **v2 版本**，已被 v3 版本替代 | 无影响，可删除 |

### 1.2 保留的 Python 文件

| 文件 | 用途 | 状态 |
|------|------|------|
| `multi_free_api_proxy/multi_free_api_proxy_v3.py` | v3 原版（备选） | 保留 |
| `multi_free_api_proxy/multi_free_api_proxy_v3_optimized.py` | **v3 优化版（推荐）** | 保留 |
| `start_all_services_gui.py` | GUI 启动器 | 保留 |
| `daemon.py` | 守护进程 | 保留 |
| `server_diagnostic.py` | 诊断工具 | 保留 |
| `doc_generator.py` | 文档生成器 | 保留 |

---

## 二、建议删除的 BAT 文件

### 2.1 过时/重复的主服务启动脚本

| 文件 | 原因 | 推荐操作 |
|------|------|----------|
| `start_multi_free_api.bat` | **v1/v2 版本启动脚本**，已过时 | **删除** |
| `start_multi_free_api_v3-ansi.bat` | 与 `start_multi_free_api_v3.bat` 相同，但**编码问题导致中文乱码** | **删除** |
| `_start_multi_free_api_v3-ansi.bat` | 下划线开头 + 乱码版本 | **删除** |
| `_DelayStart-auto_start_multi_free_api_v3-ansi.bat` | 下划线开头 + 乱码 + 延迟启动 | **删除** |
| `start_main_service.bat` | 与 `start_multi_free_api_v3.bat` **完全相同** | **删除** |
| `010-start_proxy_daemon-start.bat` | 与 `start_proxy_daemon.bat` 重复（简化版 vs 完整版） | **删除** |
| `start_doc_generator.bat` | **仅显示提示信息，无实际功能** | **删除** |

### 2.2 保留的 BAT 文件

| 文件 | 用途 | 状态 |
|------|------|------|
| `start_proxy.bat` | 启动 v1 原始版（可选保留） | 保留 |
| `start_multi_free_api_v3.bat` | **启动 v3 原版** | 保留 |
| `_start_multi_free_api_v3-optimized.bat` | **启动 v3 优化版（推荐）** | 保留 |
| `start_proxy_daemon.bat` | 守护进程管理 | 保留 |
| `035-start_all_services.bat` | 启动所有服务 | 保留 |
| `036-start_all_services_gui.bat` | GUI 启动器 | 保留 |
| `050-start_api_proxy_go.bat` | 启动 Go 版 | 保留 |
| `020-test-on5001.bat` | 测试脚本 | 保留 |
| `040-test-api-proxy-go.bat` | Go 版测试脚本 | 保留 |
| `test_main_service.bat` | 测试脚本 | 保留 |
| `test_free5_service.bat` | 测试脚本 | 保留 |
| `test_free8_service.bat` | 测试脚本 | 保留 |

---

## 三、scenarios 目录分析

### 3.1 目录内容

```
scenarios/
├── development/      # 开发环境相关
├── production/       # 生产环境相关
├── simple-deployment/ # 简单部署
├── win2012-server/   # Win2012 服务器优化
├── README.md
```

### 3.2 建议

| 内容 | 建议 | 原因 |
|------|------|------|
| `scenarios/` 整个目录 | **考虑删除或移动到存档** | 包含历史部署脚本，部分脚本（如 win2012-server）是针对旧系统 |
| 如果仍有使用 | 保留 | 如果在生产环境中使用 |

---

## 四、v1 目录分析

### 4.1 目录内容

```
v1/
├── 一键启动.bat
├── index.html
├── kuaecloud-api.php      # PHP 版本的 API
├── README.md
├── README_CN.md
├── start.bat
├── test-api.html
└── kuaecloud-coding_plan-tools_config.txt
```

### 4.2 建议

| 内容 | 建议 | 原因 |
|------|------|------|
| `v1/` 整个目录 | **保留但可标记为"已归档"** | PHP 版本独立项目，与 Python 版本并行 |
| `v1/README.md` | 保留 | 说明 PHP 版本用途 |

---

## 五、docs 目录分析

### 5.1 目录内容

```
docs/
├── api_key_info.json      # API Key 信息
├── openrouter_free_models_guide.html
└── openrouter_quickstart_guide.html
```

### 5.2 建议

| 内容 | 建议 | 原因 |
|------|------|------|
| `docs/api_key_info.json` | **检查是否需要更新**，确认后决定 |
| `docs/*.html` | 保留 | OpenRouter 使用指南 |

---

## 六、确认删除清单

### 6.1 Python 文件（2个）

```
local_api_proxy.py
multi_free_api_proxy/multi_free_api_proxy.py
```

### 6.2 BAT 文件（7个）

```
start_multi_free_api.bat
start_multi_free_api_v3-ansi.bat
_start_multi_free_api_v3-ansi.bat
_DelayStart-auto_start_multi_free_api_v3-ansi.bat
start_main_service.bat
010-start_proxy_daemon-start.bat
start_doc_generator.bat
```

### 6.3 可选：scenarios 目录

```
scenarios/
```

---

## 七、操作命令

### 7.1 删除 Python 文件

```batch
cd d:\ks_ws\git-root\ks1-simple-api
del local_api_proxy.py
del multi_free_api_proxy\multi_free_api_proxy.py
```

### 7.2 删除 BAT 文件

```batch
del start_multi_free_api.bat
del start_multi_free_api_v3-ansi.bat
del _start_multi_free_api_v3-ansi.bat
del _DelayStart-auto_start_multi_free_api_v3-ansi.bat
del start_main_service.bat
del 010-start_proxy_daemon-start.bat
del start_doc_generator.bat
```

### 7.3 移动 scenarios 目录到存档

```batch
if exist scenarios move scenarios .sisyphus\scenarios-backup
```

---

## 八、删除后的文件结构

```
ks1-simple-api/
├── .env
├── start_all_services_gui.py          # GUI 启动器
├── daemon.py                           # 守护进程
├── server_diagnostic.py                # 诊断工具
├── doc_generator.py                    # 文档生成器
│
├── multi_free_api_proxy/               # 主服务目录
│   ├── multi_free_api_proxy_v3.py      # v3 原版（备选）
│   ├── multi_free_api_proxy_v3_optimized.py  # v3 优化版（推荐）
│   ├── config.py
│   ├── app_state.py
│   ├── errors.py
│   └── __init__.py
│
├── free_api_test/                      # 上游配置
│   ├── free1/ ~ free21/
│   └── _template/
│
├── api-proxy-go/                       # Go 版本
│   ├── main.go
│   ├── config.yaml
│   └── ...
│
├── v1/                                 # PHP 版本（归档）
│   └── ...
│
├── docs/                               # 文档
│   └── ...
│
├── 启动文件与版本关系.md
├── operateLog.md
└── *.bat                               # 保留的启动脚本
```

---

**请确认是否执行以上删除操作。**
