---
trigger: model_decision
description: OPERATELOG 追加写入强制规则
---
OPERATELOG 追加写入强制规则
更新时间：2025-10-03 22:00:00

1. 目的
- 确保 operateLog.md 永远只“追加”，绝不覆盖，避免历史记录丢失。

2. 强制约束
- 禁止直接覆盖写入 operateLog.md。
- 每次记录必须遵循：先读取原内容 → 在内存中拼接新条目 → 将合并后的完整内容写回。
- 若读取失败或返回空内容，必须先确认原因，严禁空内容覆盖写回。

3. 时间规范
- 使用本地时间，格式：
  - PHP：date('Y-m-d H:i:s')
  - Python：datetime.now().strftime('%Y-%m-%d %H:%M:%S')

4. 日志条目格式（示例）
## 2025-10-03 22:00:00
- 修改文件：相对路径
- 目的：简述目标
- 变更要点：要点1、要点2
- 影响范围：可选
- 校验建议：可选

5. 故障处理
- 一旦发现 operateLog.md 被覆盖，立即创建“修复说明”条目（追加写入），描述丢失风险和后续承诺。
- 同时回溯本次会话内的关键动作，尽可能补全记录。

6. 实施要求
- 工具调用层面：必须先 read_file，再 write_to_file（写入内容包含“旧内容 + 两个换行 + 新条目”）。
- 禁止直接使用仅写入工具去替换原文件内容。