name: "free5"
address: "https://aigw-gzgy2.cucloud.cn:8443"  # 注意：这里不添加 /v1
api_key: ""  # 从环境变量 FREE5_API_KEY 加载 (iFlow SDK, 已停用)
enabled: false
default_weight: 150

# 限额配置（根据实际情况调整）
limit:
  hourly: 1000
  daily: 5000
  monthly: 100000

# 阈值配置
thresholds:
  warning: 80
  critical: 95

# 代理配置
use_proxy: false
use_sdk: false

# 模型配置
model: "unicom-cloud/MiniMax-M2.5"
available_models:
  - "unicom-cloud/MiniMax-M2.5"
use_weighted_model: false

# 响应格式配置
response_format:
  content_fields: ['content']
  merge_fields: false
  use_reasoning_as_fallback: false

# 最大 token 数
max_tokens: 2000
